services:
  - type: web
    name: ai-job-scraper
    env: docker
    plan: free
    dockerContext: ./scraper
    dockerfilePath: ./scraper/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
